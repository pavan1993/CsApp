# Database
# IMPORTANT: Copy this file to .env and update with your actual PostgreSQL credentials

# Option 1: Default PostgreSQL user with password
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/customer_success_db?schema=public"

# Option 2: Custom user with password
# DATABASE_URL="postgresql://your_username:your_password@localhost:5432/customer_success_db?schema=public"

# Option 3: User without password (if your PostgreSQL setup doesn't require one)
# DATABASE_URL="postgresql://your_username@localhost:5432/customer_success_db?schema=public"

# Option 4: If using default PostgreSQL setup on macOS with Homebrew
# DATABASE_URL="postgresql://$(whoami)@localhost:5432/customer_success_db?schema=public"

# Server
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000
